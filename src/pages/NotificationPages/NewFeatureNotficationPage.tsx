import React from 'react';
import MainTemplate from '../../templates/MainTemplate/MainTemplate.tsx';

const NewFeatureNotificationPage: React.FC = () => (
  <MainTemplate>
    <div>"This is a new feature notification page."</div>
  </MainTemplate>
);

export default NewFeatureNotificationPage;
