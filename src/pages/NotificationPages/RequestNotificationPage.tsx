import React from 'react';
import MainTemplate from '../../templates/MainTemplate/MainTemplate.tsx';

const RequestNotificationPage: React.FC = () => (
  <MainTemplate>
    <div>"This is a request notification page"</div>
  </MainTemplate>
);

export default RequestNotificationPage;